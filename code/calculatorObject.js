const add = (x, y) => {
    return x + y;
}

const sub = (x, y) => {
    return x - y;
}

const multi = (x, y) => {
    return x * y;
}

const div = (x, y) => {
    return x / y;
}

export { add, sub, multi, div }